import React from 'react';

const Destination = () => {
    return (
        <div>
            <h1 style={{textAlign: 'center'}}>My Destination is become a Web Developer.</h1>
        </div>
    );
};

export default Destination;
